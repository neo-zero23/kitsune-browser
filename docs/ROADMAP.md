# Roadmap — Kitsune Browser

## Stable
- **Yang 0.1.0** (LTS): navegador simple, Tauri 2. Instalación por releases
  (Windows .exe, Linux .deb/.rpm/.AppImage). Solo fixes críticos.

## Icebox (sin fecha)
- **Yin**: navegador avanzado en Electron (código v1 en `apps/yin/`).
- **Launcher**: selector de edición (nunca implementado, ver `apps/launcher/`).
- Tab discarding, containers, split view, Glance, command palette.
- Paquete AUR (`kitsune-yang-bin`).
